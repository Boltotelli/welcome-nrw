name: NAP Tracker V2 — GitHub Pages

on:
  push:
    branches: [main]
    paths:
      - ".github/workflows/nap2-pages.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: nap2-pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Checkout isolated V2 branch
        uses: actions/checkout@v4
        with:
          ref: fix/nap2-navigation-mobile
      - name: Validate and stage V2
        shell: bash
        run: |
          test -s nap-event-tracker-test/index.html
          test -s nap-event-tracker-test/live-adapter.js
          mkdir -p "$RUNNER_TEMP/nap2-site"
          cp -R nap-event-tracker-test/. "$RUNNER_TEMP/nap2-site/"
          touch "$RUNNER_TEMP/nap2-site/.nojekyll"
      - name: Configure Pages
        uses: actions/configure-pages@v5
        with:
          enablement: true
      - name: Upload V2 artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: ${{ runner.temp }}/nap2-site
      - name: Deploy V2
        id: deployment
        uses: actions/deploy-pages@v4
# Refresh isolated V2: mobile header, notifications, profile tabs, Crown and KvK dates
# Refresh isolated V2: inline evidence, profile comments, mobile alliance login and Crown access
# Publish V2 evidence/login/Crown + adapter cache refresh 2026-09-24

# Deploy isolated V2 native OCR, SG days, manual attendance and performance import 2026-09-24

# Publish OCR statuses, dotted scores and manually added players 2026-09-24

# Deploy alliance-first manual OCR selection and tracked-score status 2026-09-24

# Redeploy refined historical score label 2026-09-24

# Publish red/amber/green OCR result cards and compact exemptions 2026-09-24

# Publish editable recording date, readable save result, and mobile login width 2026-09-24

# Publish consistent stage-1 contact, Kingshot avatars and simplified player filters 2026-09-24

# Deploy secure avatar proxy loading and resilient picture fallback 2026-09-24

# Deploy API fallback for expired avatar CDN links and V7 mockup reference 2026-09-24

# Publish redirected API player portraits and compact V2 Home cards 2026-09-24

# Publish V2-only checked NAP notifications and live Home priority panels 2026-09-24

# Publish consistent dynamic language handling across all V2 views 2026-09-24

# Deploy complete four-language V2 dynamic UI translations after screenshot audit 2026-09-24

# Publish verified V2 i18n and privacy-filtered kingdom audit feed 2026-09-24

# Deploy verified complete V2 locale fixes and paginated all-alliance audit 2026-09-24

# Deploy explicit post-contact spending confirmation flow 2026-09-24
# Publish NRW V2 without Shared Spending demo; isolated branch, Pages only 2026-09-24
# Publish isolated V2 unified sanction-stage projection 2026-09-24
# Publish isolated V2 SG UTC 3x and 4x threshold display 2026-09-24
# Publish V2 sanction timer/status consistency 2026-09-25
# Publish V2 evidence retry/backfill 2026-09-25
# Publish V2 server-synced notification read state 2026-09-25
# Publish V2 i18n stability and Home API sync status 2026-09-25
# Publish V2 server-side activity filters and privacy-safe pagination 2026-09-25
# Publish CWR resequence fix and overdue duration formatting 2026-09-25
# Publish V2 Level 4 start-driven status 2026-09-25
# Publish V2 Level4 hosting, foreign tracking, evidence required 2026-09-25
# Publish private cross-alliance ScreenImport corrections 2026-09-25
# Publish V2 mandatory violation delete reason 2026-09-25
# Publish Crown cleanup and expiry resequence follow-up 2026-09-25
# Publish V2 Level4 bell notifications 2026-09-25
# Publish V2 final usability pass 2026-09-25
# Publish V2 confirmed-transfer guard for new NAP arrivals 2026-09-25
# Publish V2 import-only ScreenRecording corrections 2026-09-25
# Publish V2 mobile API sync spacing and light-mode red contrast 2026-09-25
